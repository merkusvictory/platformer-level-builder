# Backend API base URL.
# In development, leave this empty and the Vite proxy handles routing to localhost:3000.
# In production (e.g. Railway backend + Vercel frontend), set this to your backend URL:
#   VITE_API_URL=https://your-backend.up.railway.app
VITE_API_URL=
